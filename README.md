# PyLearning

Course work from the Zero-to-Agentic AI Syllabus, one folder per course, in the order the syllabus goes: Python fundamentals -> practice -> math -> ML -> deep learning -> LLMs -> agentic AI -> Claude -> SQL -> mobile UI -> API integration -> deployment.

`01-cs50p/` is intentionally left out of version control here — Harvard's academic honesty policy asks that CS50P solutions not be published publicly, so that work stays local (or in its own private repo).
